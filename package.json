{
  "name": "simple-json-request",
  "version": "0.4.0",
  "description": "A simple json request module with Promise support.",
  "main": "lib/SimpleJsonRequest.js",
  "scripts": {
    "unit": "tap test/*.test.js",
    "test": "npm run lint && npm run unit",
    "lint": "standard index.js lib/* test/*",
    "coveralls": "npm run unit --  --cov",
    "coverage-report": "npm run coveralls && tap --coverage-report=lcov"
  },
  "keywords": [
    "request",
    "json",
    "promise",
    "http"
  ],
  "engines": {
    "node": ">= 6.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SerayaEryn/simple-json-request.git"
  },
  "author": "Denis Fäcke",
  "license": "MIT",
  "dependencies": {
    "simple-get": "^3.0.2"
  },
  "devDependencies": {
    "coveralls": "^3.0.0",
    "standard": "^12.0.1",
    "tap": "^12.0.0"
  }
}
